FROM golang:1.25-alpine AS builder

WORKDIR /app
COPY . .
RUN go mod download
RUN go build -o iam .

FROM alpine:latest
WORKDIR /app
COPY --from=builder /app/iam .
RUN mkdir -p /app/keys

ENV IAM_CONFIG_LOCATION=/app/config.json
EXPOSE 8081

CMD ["./iam"]
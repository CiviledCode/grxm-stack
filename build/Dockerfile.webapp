FROM golang:1.25-alpine AS builder

WORKDIR /app
COPY . .
RUN go mod download
RUN go build -o webapp .

FROM alpine:latest
WORKDIR /app
COPY --from=builder /app/webapp .

ENV API_CONFIG_LOCATION=/app/config.json
EXPOSE 8080

CMD ["./webapp"]